# Copie para ".env" na raiz do projeto (o ficheiro .env não deve ir para o Git).
# Em desenvolvimento (npm run dev) usa-se primeiro VITE_N8N_WEBHOOK_TEST; em build de
# produção (npm run build) usa-se primeiro VITE_N8N_WEBHOOK_PROD.

VITE_N8N_WEBHOOK_TEST=
VITE_N8N_WEBHOOK_PROD=
